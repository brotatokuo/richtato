# Docker development - when both frontend and backend are in containers
# Note: Browser requests go to localhost since they run on host, not in container
VITE_API_BASE_URL=http://localhost:8000/api/v1
